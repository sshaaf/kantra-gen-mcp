package dev.shaaf.kantra.rules.gen.model;

public enum JavaLocation {
    INHERITANCE,
    METHOD_CALL,
    CONSTRUCTOR_CALL,
    ANNOTATION,
    IMPLEMENTS_TYPE,
    ENUM,
    RETURN_TYPE,
    IMPORT,
    VARIABLE_DECLARATION,
    TYPE,
    PACKAGE,
    FIELD,
    METHOD,
    CLASS
}